# Public marketing surface (unauthenticated, SEO-driven)

`layout.tsx` (Navbar + Footer shells, no business nav links yet),
`error.tsx`/`loading.tsx` are real. No business pages yet — see
`docs/architecture/application-runtime.md`.

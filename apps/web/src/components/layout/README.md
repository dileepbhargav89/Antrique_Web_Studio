# Layout shells

`container.tsx`, `grid.tsx` (responsive primitives — see
`docs/architecture/design-system.md` §2), `navbar.tsx`, `sidebar.tsx`
(structural shells with content slots, zero nav items — see §3). No page
layout composition happens here; that's a future feature phase.

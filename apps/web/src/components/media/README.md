# Media foundation

`image.tsx` (thin `next/image` wrapper + an `aspectRatio` prop),
`video.tsx` (native `<video>`, IntersectionObserver-gated lazy loading,
required `poster`). No production assets. See `docs/architecture/
design-system.md` §8.

import { createQueryKeys } from '@/lib/query/query-keys';

export const categoryKeys = createQueryKeys('catalog-categories');
export const collectionKeys = createQueryKeys('catalog-collections');
export const productKeys = createQueryKeys('catalog-products');

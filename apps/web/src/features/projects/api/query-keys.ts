import { createQueryKeys } from '@/lib/query/query-keys';

export const projectKeys = createQueryKeys('projects');
export const milestoneKeys = createQueryKeys('milestones');
export const taskKeys = createQueryKeys('tasks');
export const commentKeys = createQueryKeys('comments');
export const documentKeys = createQueryKeys('project-documents');
export const activityKeys = createQueryKeys('project-activity');

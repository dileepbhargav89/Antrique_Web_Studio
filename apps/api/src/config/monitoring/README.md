# Monitoring configuration

Placeholder — describes the purpose of this directory. No implementation.

Observability configuration — Sentry DSN, OpenTelemetry exporter endpoint —
see `.env.example`'s `SENTRY_DSN`/`OTEL_EXPORTER_OTLP_ENDPOINT`.

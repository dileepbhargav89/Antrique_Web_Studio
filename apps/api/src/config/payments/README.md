# Payments configuration

Placeholder — describes the purpose of this directory. No implementation.

Hosted payment gateway configuration (public/secret keys, webhook secret) —
see `.env.example`'s `PAYMENT_*` variables. Never raw card/credential data,
per CLAUDE.md.

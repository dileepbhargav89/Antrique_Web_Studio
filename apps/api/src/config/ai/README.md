# AI configuration

Phase 8 (AI Workspace) — provider API keys/model selection for the four
adapters in `apps/api/src/ai/`. See `ai.config.ts` and `env.validation.ts`'s
own AI section; every key is optional (reduced capability, not a boot
failure — same treatment `email`/`storage` config already get).

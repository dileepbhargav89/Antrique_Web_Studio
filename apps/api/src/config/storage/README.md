# Storage configuration

Placeholder — describes the purpose of this directory. No implementation.

Object storage configuration (bucket, region, credentials namespace) for
pre-signed uploads — see `.env.example`'s `STORAGE_*` variables.

# Auth configuration

Placeholder — describes the purpose of this directory. No implementation.

Managed IdP / JWT / session configuration (issuer URL, client id/secret,
token TTLs) — see `.env.example`'s `IDP_*`/`JWT_*` variables. Distinct from
`security/` (cross-cutting policy, not identity).

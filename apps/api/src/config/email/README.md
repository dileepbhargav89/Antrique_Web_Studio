# Email configuration

Placeholder — describes the purpose of this directory. No implementation.

Transactional email provider (ESP) configuration — API key, from-address.
Distinct from `notifications/` (which channel/when to notify, not the
email transport itself).

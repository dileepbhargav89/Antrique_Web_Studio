# Pipes

Placeholder — describes the purpose of this directory. No implementation.

Request validation (`ValidationPipe` against DTO/shared validation schemas)
lands here in Phase 1.2B — see `main.ts`'s bootstrap comment for exactly
where it registers.

# Mappers

Placeholder — no mapper exists yet in this reference module (nothing to
map: no entity, no persistence model). A real domain module adds pure
functions/classes here that convert between representations — Prisma
model → entity, entity → response DTO — so conversion logic lives in
exactly one place instead of scattered across services and controllers.

Full convention: docs/architecture/domain-module-guide.md.

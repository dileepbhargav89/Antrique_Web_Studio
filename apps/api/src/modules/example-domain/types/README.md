# Types

Placeholder — no standalone type exists yet in this reference module.
Types here are plain TypeScript aliases/utility types that are neither a
validated wire shape (`dto/`) nor a domain object with identity
(`entities/`) — e.g. a literal union, a mapped type, a narrow helper
type used only inside this module.

Full convention: docs/architecture/domain-module-guide.md.

# Validators

Placeholder — no custom validator exists yet. `dto/login-request.dto.ts`/
`dto/refresh-request.dto.ts`'s rules are fully expressed by
`class-validator`'s built-in decorators (`@IsEmail()`, `@IsString()`,
`@MinLength()`) — nothing yet needs a rule those can't express.

Full convention: docs/architecture/domain-module-guide.md.

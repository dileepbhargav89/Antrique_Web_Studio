# Placeholder — no apps/worker exists yet. The architecture calls for a
# queue/async worker (see docs/architecture/architecture.md "Backend"), but
# that app hasn't been scaffolded. Model this file on api.Dockerfile once
# apps/worker exists: same deps/dev/build/runtime stages, CMD pointed at the
# worker's entrypoint instead of dist/main.js.
